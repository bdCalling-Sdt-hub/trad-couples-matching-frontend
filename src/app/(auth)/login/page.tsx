import React from 'react';
import LoginClient from '../../../components/ui/auth/Login/LoginClient';

const page = () => {
    return (
        <div>
 <LoginClient />
        </div>
    );
};

export default page;
import React from 'react';
import RegisterClient from './RegisterClient';

const page = () => {
    return (
        <div>
            <RegisterClient />
        </div>
    );
};

export default page;
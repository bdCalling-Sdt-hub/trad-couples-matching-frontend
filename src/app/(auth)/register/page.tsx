import React from 'react';
import RegisterClient from '../../../components/ui/auth/Register/RegisterClient';

const page = () => {
    return (
        <div>
            <RegisterClient />
        </div>
    );
};

export default page;
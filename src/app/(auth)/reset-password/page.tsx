import React from 'react';
import ResetPasswordClient from './ResetPasswordClient';

const page = () => {
    return (
        <div>
            <ResetPasswordClient />
        </div>
    );
};

export default page;
import React from 'react';
import ChatClient from '../../../components/ui/website/Chat/ChatClient';

const page = () => {
    return (
        <div>
            <ChatClient />
        </div>
    );
};

export default page;
import React from 'react';
import HomeClient from '../../../components/ui/website/Home/HomeClient';

const page = () => {
    return (
        <div>
            <HomeClient />
        </div>
    );
};

export default page;
import React from 'react';
import SubscriptionsClient from './SubscriptionsClient';

const page = () => {
    return (
        <div>
             <SubscriptionsClient/>
        </div>
    );
};

export default page;